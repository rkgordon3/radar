

<div class="relationship-checklist">
<% if report.class.name == "IncidentReport" %>
<%= link_to image_tag("/images/add_icon.gif", :id => "#{relationship.participant_id}-img"), "javascript:void(0)",  
                     :onclick => "divToggle('#{relationship.participant_id}-infractions', '#{relationship.participant_id}-img');" %>
 
  	    <b>Infractions:</b>
  	<div id="<%= relationship.participant_id %>-infractions" style="display: none;">
  			<% infractions = RelationshipToReport.all %>
  			<% infractions.each do |infraction| %>
  		  		<div style="margin-left: 48px;margin-top: 4px; margin-bottom: 0px;" >
  						<%= check_box_tag "#{relationship.participant_id}[#{infraction.id}]", 
  				       infraction.description,
  				       report.get_specific_report_student_relationship(relationship.participant_id, infraction.id) != nil %>
  				       
  				    <%= label_tag infraction.description %>			
  				
  				  </div>
  			<% end %>
  	</div>
<% end %>
</div>
