<% if(type == "edit") %>
        <b><%= f.label :building_id %>:<br /></b>
		<%= select :report, :building_id, options_from_collection_for_select(BuildingsHelper::all_buildings, "id", "name", (report.building.nil? ? "" : report.building.id)), :include_blank => true %>
<% elsif(type == "search") %>
    <b>Building:</b><br />
    <%= select_tag "building_id", options_from_collection_for_select(BuildingsHelper::all_buildings, "id", "name"), :include_blank => true %>
<% elsif(type == "index") %>

<% elsif(type == "show") %>
    <%= label_tag  do -%>
	    <%= t "#{current_staff.organization}.location" %>:
	<% end -%>
    <%= @report.building_name  %>
<% end %>