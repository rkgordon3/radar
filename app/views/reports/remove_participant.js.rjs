page.replace_html("s-i-form", :partial => "participants_in_report", :locals => { :width => 3, :report => @report, :participants => Participant.where(:id => @report.participant_ids).order(:last_name) })
if @report.participant_ids.size < 2
  page.hide 'common-reasons-container'
end

