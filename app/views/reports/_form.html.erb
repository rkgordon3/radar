<%= form_for(@report) do |f| %>
  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Approach Time<br />
    <%= f.text_field :approach_time %><br/>
  </div>
  <div class="field">
    <%= f.label :annotation %><br />
    <%= f.text_area :annotation %><br/>
  </div>
  <div class="field">
    Staff ID<br />
    <%= f.text_field :staff_id %><br/>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
