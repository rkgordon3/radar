<%= form_for(@building) do |f| %>
  <% if @building.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
      <% @building.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :abbreviation %><br />
    <%= f.text_field :abbreviation %>
  </div>
  <div class="field">
    Area<br />
    <% areas = Area.all %>
	<% selected = @building.area_id %>
	<% if selected == nil %>
		<% selected = Building.unspecified_id %>
	<% end %>
    <%= render :partial => "areas/all_areas_select_tag", :locals => {:selected_id => selected }%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
