<%= form_for(@student_infraction) do |f| %>
  <% if @student_infraction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_infraction.errors.count, "error") %> prohibited this student_infraction from being saved:</h2>

      <ul>
      <% @student_infraction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time %><br />
    <%= f.text_field :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
