<%= form_for(@relationship_to_report) do |f| %>
  <% if @relationship_to_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relationship_to_report.errors.count, "error") %> prohibited this contact reason from being saved:</h2>

      <ul>
      <% @relationship_to_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
	
  </div>
  <div  class="field" >
  <label><%= t 'select_a_report_for_infraction' %></label><br/>
  <%= select(:relationship_to_report,
		:report_type_id, ReportTypesHelper::for_select, 
		html_options = { :selected => @relationship_to_report.report_type.nil? ? ReportTypesHelper::all_id : @relationship_to_report.report_type_id.to_s } )%>
  </div>
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
