<%= form_for(@relationship_to_report) do |f| %>
  <% if @relationship_to_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relationship_to_report.errors.count, "error") %> prohibited this relationship to report from being saved:</h2>

      <ul>
      <% @relationship_to_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
