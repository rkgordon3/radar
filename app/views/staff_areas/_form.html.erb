<%= form_for(@staff_area) do |f| %>
  <% if @staff_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@staff_area.errors.count, "error") %> prohibited this staff_area from being saved:</h2>

      <ul>
      <% @staff_area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :staff_id %><br />
    <%= f.text_field :staff_id %>
  </div>
  <div class="field">
    <%= f.label :area_id %><br />
    <%= f.text_field :area_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
